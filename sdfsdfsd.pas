//Используй алгоритм!!! Нужно только изменить условие выхода из рекурсии.

const s = 9; //И что она делает? Определи константу matrix='0123456789'
var
Arr: array of integer;
procedure happy(tic,p:integer; a: array of integer);
begin
if tic = 6 then //Выход, когда длина скомпонованной строки равна 6 + "счастливое" условие.
  begin
    if (A[0]+A[1]+A[2])=(A[3]+A[4]+A[5]) then  //Используй функции StrToInt() и Copy()
    p:=p+1;
writeln (p);
end;
end;
begin
  Arr:=new integer[s];
end.